services:
  augur:
    image: ghcr.io/mayvqt/augur:latest
    build: .
    container_name: augur
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./data:/data
