# Features

- `/request` searches and submits movie or TV requests.
- `/requests` shows a linked user's bounded recent Seerr request history.
- `/setup` publishes approval cards; decisions support optional local decline reasons.
- `/notifications` controls approved, declined, and available DMs (all enabled by default).
- A background monitor tracks availability and reconciles approval state.
